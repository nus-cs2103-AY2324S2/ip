plugins {
    id 'java'
    id 'application'
    id 'com.github.johnrengelman.shadow' version '7.1.2'
}

repositories {
    mavenCentral()
}

dependencies {
    // Junit
    testImplementation group: 'org.junit.jupiter', name: 'junit-jupiter-api', version: '5.10.0'
    testRuntimeOnly group: 'org.junit.jupiter', name: 'junit-jupiter-engine', version: '5.10.0'
}

test {
    useJUnitPlatform()
    exclude '**/TaskListTest.class'

    testLogging {
        events "passed", "skipped", "failed"

        showExceptions true
        exceptionFormat "full"
        showCauses true
        showStackTraces true
        showStandardStreams = false
    }
}

tasks.withType(Test) {
    // Exclude TaskListTest from the test execution
    exclude '**/TaskListTest.class'
}

//application {
//    mainClass.set("seedu.duke.Duke")
//}

// to fix Error: Could not find or load main class seedu.duke.Duke
// use package path
application {
    mainClassName = 'bytebuddy.ByteBuddy'
}

// JAR output is in build/libs
shadowJar {
    archiveBaseName = "bytebuddy"
    archiveClassifier = null
    dependsOn("distZip", "distTar")
    archiveFileName = 'bytebuddy.jar'
}

run {
    standardInput = System.in
}
