package jav.command;
/**
 * A type of command that handles the execution of task related commands.
 */
public abstract class TaskCommand extends Command {
    /** The parameters involving the command. */
    protected String param;
}
