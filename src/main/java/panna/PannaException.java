package panna;

/**
 * Encapsulates all the exceptions in the PannaBot running
 */
public class PannaException extends Exception {
    public PannaException(String error) {
        super(error);
    }
}
