package checkbot;

import javafx.application.Application;

/**
 * Represents the launcher that launches the GUI for Checkbot.
 */
public class Launcher {
    public static void main(String[] args) {
        Application.launch(Checkbot.class, args);
    }
}
