package Duke.UI;

import javafx.application.Application;

/**
 * Runs the JavaFX GUi for the user interface
 */
public class Launcher {
    public static void main(String[] args) {
        Application.launch(MainWindow.class, args);
    }
}
