package shirmin;
import javafx.application.Application;

/**
 * A launcher class to launch the main app
 */
public class Launcher {
    public static void main(String[] args) {
        Application.launch(MainApp.class, args);
    }
}