/**
 * Represents a command to delete a task
 */
public class DeleteTaskCommand extends Command {
    /**
     * {@inheritDoc}
     *
     * Deletes the task from the TaskList, saving the resulting TaskList
     */
    @Override
    public void execute(TaskList tasks, Storage storage) {
        return;
    }
}
