package duke.task;

/**
 * Enum class that represents types of tasks. This is used to make task types a formal value rather than strings.
 */
public enum TaskType {
    TODO,
    EVENT,
    DEADLINE
}
