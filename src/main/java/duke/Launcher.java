package duke;

import javafx.application.Application;

/**
 * Serves as the entry point for the program.
 */
public class Launcher {
    public static void main(String[] args) {
        Application.launch(Stille.class, args);
    }
}
