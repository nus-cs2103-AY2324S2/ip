package ukecat;

/**
 * Enum representing the types of marking for tasks.
 * - MARK: Indicates marking a task as done.
 * - UNMARK: Indicates marking a task as not done.
 */
public enum MarkType {
    MARK, UNMARK;
}
