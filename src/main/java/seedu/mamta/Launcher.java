package seedu.mamta;

import javafx.application.Application;

/**
 * A launcher class to workaround classpath issues. Launches the UI.
 */
public class Launcher {
    public static void main(String[] args) {
        Application.launch(Mamta.class, args);
    }
}