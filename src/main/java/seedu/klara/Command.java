package seedu.klara;

/**
 * Represents <code>Enum</code> values corresponding to
 * potential commands from user input e.g., <code>list</code>
 */
public enum Command {
    // found enums to be unnecessary using while loop and if-else loop
    LIST, MARK, UNMARK, TODO, DEADLINE, EVENT, DELETE, BYE, FIND, COMMAND
}
